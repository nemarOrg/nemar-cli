{
  "name": "nemar-api",
  "version": "0.8.24-dev1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dev": "wrangler deploy --env development",
    "db:migrate": "wrangler d1 migrations apply nemar-db",
    "db:migrate:local": "wrangler d1 migrations apply nemar-db --local",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0",
    "@hono/zod-validator": "^0.4.0",
    "bcryptjs": "^2.4.3",
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "wrangler": "^4.85.0",
    "typescript": "^5.6.0",
    "@types/bcryptjs": "^2.4.6"
  }
}
