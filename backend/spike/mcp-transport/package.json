{
  "name": "mcp-transport-spike",
  "private": true,
  "version": "0.0.0",
  "description": "Phase 1 spike (issue #1293): does the v2 MCP SDK run on workerd, and which blosc/zstd decode path does render_overview/read_window's taste use? Replaced by phase 2 (#1294) and deleted in that PR -- see README.md.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir dist-dry-run"
  },
  "dependencies": {
    "@modelcontextprotocol/hono": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "fzstd": "0.1.1",
    "hono": "^4.11.4",
    "numcodecs": "0.3.2",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250906.0",
    "typescript": "^5.6.0",
    "wrangler": "4.129.1"
  }
}
